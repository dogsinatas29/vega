# 🌌 VEGA 릴리즈 노트: v0.0.17.14 (환경 인지 추론 강화)

## 🚀 주요 업데이트 사항

### 1. 환경 인지형 타겟 추론 (Environment-Aware Cognition)
VEGA가 이제 사용자의 인프라 문맥을 깊이 있게 이해합니다.
- **휴리스틱 그라운딩**: "ssh 연결된 시스템", "원격 머신", "현재 노드" 등 추상적인 표현을 지식 베이스(KB)의 실제 호스트로 지능적으로 매핑합니다.
- **컨텍스트 주입**: AI 프롬프트에 [사용자, IP, 포트, 상태]를 포함한 상세 인벤토리 메타데이터를 주입하여 추론 정확도를 극대화했습니다.
- **오염 방지**: 원격지 대상 작업이 명확한 근거 없이 `localhost`로 폴백되는 것을 방지하여 로컬 시스템 안전성을 확보했습니다.

### 2. SSH RAW 가시성 강화 (Observability)
"정제된 보고"를 넘어 "시스템 투명성"을 지향합니다.
- **직접 스트림 캡처**: 로우 레벨 디버그 블록을 주입하여 파싱 전의 원본 STDOUT, STDERR, EXIT CODE를 콘솔에 다이렉트로 노출합니다.
- **정밀 진단**: 원격 명령 실패 시 시스템 레벨의 상세 에러(예: "model not found")를 즉시 확인할 수 있습니다.

### 3. Thin Action Execution (스냅샷 우회)
원자적 제어 명령에 대한 성능 최적화를 단행했습니다.
- **우회 로직**: Ollama 모델 조회/삭제 등 단순 작업 시 무거운 `HostSnapshot` 캡처 단계를 건너뜁니다.
- **지연 시간 단축**: 고빈도 인프라 관리 작업의 실행 속도가 3~5배 향상되었습니다.

### 4. 격리 및 보안 강화
- **명시적 OLLAMA_HOST 설정**: 모든 Ollama 액션에 `OLLAMA_HOST=127.0.0.1:11434` 환경 변수를 강제하여 대상 노드에서의 결정론적 실행을 보장합니다.
- **실행 디스패치 표준화**: 로컬 및 원격 트랜스포트 전반의 `ActionExecutor` 흐름을 통일했습니다.

---
**"간결함은 SRE의 영혼이며, 결정론은 그 척추이다."** 🫡🌌🚀
